# runnning

```shell
osqueryi --nodisable_extensions
osquery> select value from osquery_flags where name = 'extensions_socket';

# seperate terminal window
./spaceja3 --socket /home/name/.osquery/shell.em

```